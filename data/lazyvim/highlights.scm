(comment) @comment @spell

[
  (addition)
  (new_file)
] @diff.plus

[
  (deletion)
  (old_file)
] @diff.minus

(changes) @diff.delta

(commit) @constant

(location) @attribute

(command) @function

(argument) @variable.parameter

(filename) @string.special.path

(special) @string.special

(mode) @number

[
  (binary_change)
  (similarity)
  (file_change)
] @label

(index) @label

(score) @number
